# ═══════════════════════════════════════════════════════════════════
# ASHA AI Hospital Voice Agent — Python Dependencies
# Python: 3.11+
# Last updated: Phase 2 cleanup — removed dead deps, added missing
# ═══════════════════════════════════════════════════════════════════

# ── Core API Framework ───────────────────────────────────────────────
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
gunicorn>=21.2.0
pydantic>=2.7.0
pydantic-settings>=2.3.0
python-dotenv>=1.0.1
python-multipart>=0.0.9
aiofiles>=23.2.1
httpx>=0.27.0
requests>=2.32.0
certifi>=2024.2.2

# ── Authentication ───────────────────────────────────────────────────
PyJWT>=2.8.0

# ── Database: PostgreSQL + Migrations ────────────────────────────────
sqlalchemy>=2.0.30
alembic>=1.13.1
psycopg[binary]>=3.1.19

# ── Cache / Session: Redis ───────────────────────────────────────────
redis>=5.0.4

# ── Vector DB + RAG Pipeline ─────────────────────────────────────────
# NOTE: chromadb and langchain-chroma REMOVED — project uses Qdrant
qdrant-client>=1.9.1
langchain>=0.2.0
langchain-core>=0.2.0
langchain-community>=0.2.0
langchain-huggingface>=0.0.3
langchain-text-splitters>=0.2.0
sentence-transformers>=2.7.0
rank-bm25>=0.2.2
pypdf>=4.2.0
tiktoken>=0.7.0
numpy>=1.26.0
torch>=2.2.0

# ── LLM Providers ────────────────────────────────────────────────────
groq>=0.8.0
openai>=1.30.0

# ── Observability: LangSmith Tracing ─────────────────────────────────
langsmith>=0.1.0

# ── Voice: STT, TTS, Telephony ───────────────────────────────────────
deepgram-sdk>=3.11.0
edge-tts>=6.1.12
websockets>=12.0
twilio>=8.0.0

# ── LiveKit: Browser-based voice calls ───────────────────────────────
livekit-agents>=0.12.0
livekit-api>=0.7.0
livekit-plugins-deepgram>=0.4.0

# ── Notifications: Email + SMS ────────────────────────────────────────
# Twilio also handles SMS — no extra package needed

# ── Admin Panel + Dashboard ───────────────────────────────────────────
streamlit>=1.35.0

# ── PDF Generation (Appointment tickets) ─────────────────────────────
reportlab>=4.0.0

# ── Logging + Scheduling + Metrics ───────────────────────────────────
loguru>=0.7.2
schedule>=1.2.2
prometheus-client>=0.20.0

# ── Testing ───────────────────────────────────────────────────────────
pytest>=8.2.0
pytest-asyncio>=0.23.0
pytest-cov>=5.0.0
httpx>=0.27.0

# ── Code Quality ──────────────────────────────────────────────────────
ruff>=0.4.0

# ═══════════════════════════════════════════════════════════════════
# REMOVED (reasons documented):
# chromadb          → Replaced by Qdrant
# langchain-chroma  → Replaced by Qdrant
# mysql-connector-python → PostgreSQL migration complete
# pygame            → Not needed in production server
# pyaudio           → Not needed in production server
# google-cloud-texttospeech → Using Edge-TTS (free)
# azure-cognitiveservices-speech → Using Edge-TTS (free)
# ═══════════════════════════════════════════════════════════════════
